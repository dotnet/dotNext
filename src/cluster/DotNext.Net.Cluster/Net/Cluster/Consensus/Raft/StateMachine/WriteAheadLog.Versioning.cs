namespace DotNext.Net.Cluster.Consensus.Raft.StateMachine;

partial class WriteAheadLog
{
    private readonly uint version;
    
    private const uint Version0 = 0;

    private const uint CurrentVersion = Version0;
}